<?php

/**
 * Implements hook_theme().
 */
function ala_une_block_theme($existing, $type, $theme, $path)
{
    $variables = array(
        'ALaUne' => array(
            'variables' => [
                'nodes' => NULL,
            ],
            'template' => 'alaune',
        ),


    );
    return $variables;
}
